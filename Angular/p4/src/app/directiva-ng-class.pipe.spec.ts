import { DirectivaNgClassPipe } from './directiva-ng-class.pipe';

describe('DirectivaNgClassPipe', () => {
  it('create an instance', () => {
    const pipe = new DirectivaNgClassPipe();
    expect(pipe).toBeTruthy();
  });
});
